// InfinitAgent uses a single OpenAI-compatible endpoint (e.g. Qwen3 35B via vLLM/llama.cpp).
// Models are configured dynamically by the operator in the options page.
// Pricing is always 0 for local/private endpoints.

export const openaiCompatibleDefaultModelId = '';
